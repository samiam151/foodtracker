import React from "react";

export const SignupNotification = (props) => {
    return (
        <div className="signup--notification">
            <p>
                If you don't have an accout, register here.
            </p>
        </div>
    );
}